```
    Установку pip можно инициировать с помощью ensurepip
    
        python3 -m ensurepip
```

### возможности pip 
```
    help       : показать справку по командам
    install    : установить пакет
    download   : загрузить пакеты
    uninstall  : удалить пакеты
    freeze     : вывести список установленных пакетов в формате требований 
    list       : вывести список установленных пакетов
    show       : вывести информацию об установленных пакетах
    check      : проверить, что у установленных пакетов есть совместимые зависимости
    config     : упровлять локальной и глобальной конфигурацией
    search     : искать пакеты в PyPi
    cache      : работать с кэшем установленных пакетов pip
    wheel      : собрать пакеты по вашим требованиям
    hash       : вычислить хэш-сумму архивированных пакетов
    completion : настроить автозавершение команд в командной строке
    debug      : показать информацию для отладки 
```